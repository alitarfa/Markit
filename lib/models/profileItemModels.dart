import 'package:flutter/cupertino.dart';

class ProfileItemModels {
  int color;
  IconData iconData;
  int colorIcon;
  String title;
  int number;

  ProfileItemModels(
      this.color, this.iconData, this.colorIcon, this.title, this.number);
}
